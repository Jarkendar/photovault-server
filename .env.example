# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit secrets.

SERVER_PORT=8080

# --- Milestone 2 (DB / Exposed) ---
# POSTGRES_USER=photovault
# POSTGRES_PASSWORD=change-me
# POSTGRES_DB=photovault
